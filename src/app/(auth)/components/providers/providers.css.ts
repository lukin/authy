import { css } from '@/styled-system/css';

export const list = css({
  display: 'grid',
  gap: '4',
});

export const icon = css({
  blockSize: '6',
});

export const error = css({
  color: 'red',
});
